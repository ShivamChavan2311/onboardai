{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r rag_app/requirements.txt"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "rootDirectory": "rag_app"
  }
}
